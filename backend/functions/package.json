{
  "name": "runchain-backend",
  "version": "1.0.0",
  "scripts": {
    "serve": "firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions",
    "build": "tsc"
  },
  "dependencies": {
    "firebase-admin": "^12.0.0",
    "firebase-functions": "^4.6.0",
    "ethers": "^6.11.1",
    "axios": "^1.6.8"
  },
  "devDependencies": {
    "typescript": "^5.3.3",
    "@types/node": "^20.0.0"
  },
  "engines": { "node": "20" }
}
